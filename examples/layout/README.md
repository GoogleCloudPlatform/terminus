# Layout Demo

Walks through layout utilities: boxes, columns, grids, alignment, and a composite example.

## Run
- `go run examples/layout/main.go`
- Optional CLI client: start the server, then `./bin/terminus-cli -addr localhost:8890`

## Controls
- `n` / `p`: next / previous layout example
- `q` or `Ctrl+C`: quit
